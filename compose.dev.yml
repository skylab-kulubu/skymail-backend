services:
  postgres:
    image: postgres:18
    restart: no
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: skymail
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - postgres_data:/var/lib/postgresql

volumes:
  postgres_data: